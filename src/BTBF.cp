#include <stdnms.hp>
#include <window.hp>

void main() {
  new WINDOW window = (
    "Back To Back Forever",      // Display Name
    "btbf.adaves1.io",           // ID
    2,                            // Optimise State
  );
}
